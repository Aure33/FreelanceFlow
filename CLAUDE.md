# FreelanceFlow — Contexte projet

SaaS B2B de facturation pour indépendants (projet Ynov, évaluation RNCP). Proposition de valeur : générer un devis/facture PDF conforme (TVA, mentions légales) **en moins de 2 minutes et 3 clics**.

## Source de vérité

**`design_ref/design_handoff_freelance_flow/README.md`** = cahier des charges complet : 21 maquettes HTML hifi, architecture des routes, design tokens, modèle Prisma, règles RLS, accessibilité (RGAA/WCAG AA), sécurité, éco-conception (EcoIndex ≥ B).

Pour implémenter un écran : lire la maquette HTML correspondante dans `design_ref/design_handoff_freelance_flow/` (ex. `Factures.html`) et recréer l'UI **pixel-perfect** avec les tokens. Le design system CSS complet est dans `styles/freelance-flow.css` (même dossier).

## Stack

| Couche | Technologie |
|---|---|
| Framework | Next.js 14.2 (App Router) + TypeScript |
| Styling | Tailwind CSS v3 + shadcn/ui (style new-york, prérequis installés, aucun composant ajouté encore) |
| Auth & BDD | Supabase (PostgreSQL + RLS + Storage) — BDD + auth branchées (#4, #3) |
| ORM | Prisma 6 — installé, client `lib/prisma.ts` (pooler 6543, `connection_limit=5` — voir ⚠️ ci-dessous) |
| PDF | Puppeteer (route API serveur) — fait (#9) |
| Déploiement | Vercel |

## ⚠️ Environnement : Bun, pas Node

Node n'est PAS installé dans WSL. Utiliser **Bun** (`~/.bun/bin/bun`, normalement dans le PATH) :

```bash
bun install          # dépendances
bun run dev          # serveur de dev
bun run build        # build de prod
bunx shadcn add <c>  # ajouter un composant shadcn
```

## ⚠️ Pool de connexions Prisma

`DATABASE_URL` doit garder **`connection_limit=5&pool_timeout=20`** (cf. `.env.example`). Avec `connection_limit=1`, les pages qui lancent plusieurs requêtes **en parallèle** (le tableau de bord : `layout` → `getUsage()` + `getNavCounts()`, puis `getDashboardData()`) se sérialisent sur une seule connexion et dépassent le `pool_timeout` → `Timed out fetching a new connection from the connection pool`. **À reporter aussi dans les variables d'environnement Vercel.**

Corollaire (éco-conception) : garder le nombre d'allers-retours BDD au minimum par page. Dans `getDashboardData()`, les 2 CA du KPI 1 sont **dérivés des seaux du graphe** et `overdueCount` réutilise le `_count` de l'agrégat « en retard » (mêmes clauses `where`) — ne pas rajouter de requête sans nécessité.

## État d'avancement

### ✅ Fait
- Scaffold Next.js 14 + Tailwind v3 + ESLint (install via bun)
- **Design tokens** : `app/globals.css` — tokens oklch complets (thème clair `:root` + sombre `[data-theme="dark"]`), copie conforme de `freelance-flow.css`
- **Mapping Tailwind** : `tailwind.config.ts` — toutes les couleurs/rayons/ombres/spacing en classes
- **Polices** : Schibsted Grotesk (sans) + JetBrains Mono (mono) via `next/font/google`, variables `--font-sans`/`--font-mono`, `display: swap`
- **Thème clair/sombre** : script inline anti-flash dans `app/layout.tsx`, clé `localStorage['ff-theme']`, attribut `data-theme` sur `<html>` (toggle UI branché dans Paramètres depuis #12)
- **Shell applicatif** : `app/(app)/layout.tsx` + `components/layout/sidebar.tsx` / `topbar.tsx` — sidebar 248px (brand, nav + badges, jauge freemium, user-chip), topbar 64px sticky backdrop-blur (titre par route, recherche ⌘K, notifs)
- **Stubs** : pages placeholder pour /dashboard, /clients, /projets, /devis, /factures, /rapports, /abonnement, /parametres (`components/layout/page-placeholder.tsx`) ; `/` redirige provisoirement vers /dashboard
- **Prérequis shadcn** : `components.json`, `lib/utils.ts` (cn), clsx, tailwind-merge, cva, tailwindcss-animate, lucide-react
- **#1 Tableau de bord** (mergé) : contenu dans `components/dashboard/` (KPI ×4, graphique CA 8 mois barres empilées CSS, panneau « À traiter », factures récentes, top clients) ; données mockées dans `components/dashboard/mock-data.ts`. Primitives ajoutées : `components/ui/button.tsx` (Button shadcn Slot+cva mappé sur les tokens) et `components/icons/currency-icon.tsx` (seule icône custom ; le reste via lucide-react)
- **#2 Landing + pages légales** (mergé) : `app/(public)/page.tsx` (landing, devient `/`) + `app/(public)/legal/page.tsx` (3 docs légaux navigables par hash) ; composants `components/public/` (nav, footer, legal-docs). Bande CTA figée sombre dans les 2 thèmes. Liens `/connexion` `/inscription` en `prefetch={false}` (routes créées en #3). Texte légal : hébergement Supabase `eu-west-1 (Dublin, Irlande)` (région du projet de prod)

- **#4 BDD** (mergé) : 5 tables Supabase (`users`, `clients`, `projects`, `documents`, `document_lines`) via migration SQL versionnée `supabase/migrations/`, **RLS `user_id = auth.uid()` active + policée sur chacune** (vérifié), trigger `on_auth_user_created` → crée le profil `public.users`. Montants en **centimes entiers**, **TVA par ligne**, `user_id` sur chaque table, unicité `(user_id, number)`, RESTRICT transitif clients←projects←documents. Schéma dans `prisma/schema.prisma`. **Rappel** : Prisma bypasse la RLS → toujours filtrer `where: { userId }` dans le code applicatif. À arbitrer en #5 : champs « Nouveau client » (forme juridique, contact, CP/ville, taux TVA défaut) non encore modélisés.

- **#3 Auth** (mergé) : Supabase Auth (email/mot de passe) + 4 écrans `app/(public)/` (connexion, inscription, mot-de-passe-oublié, réinitialisation) ; infra `lib/supabase/` (clients SSR browser+server) + `middleware.ts` (protège le groupe app → non-auth redirigé vers /connexion, déjà-connecté hors des pages auth) + `lib/auth/` (server actions signIn/signUp/signOut/requestPasswordReset/updatePassword validées zod ; `requireUserId()` à utiliser dans les futures requêtes Prisma). Connexion **Google (OAuth Supabase) fonctionnelle** (#19) : `signInWithOAuth` + route `app/auth/callback/route.ts` (`exchangeCodeForSession`, flux PKCE) ; provider Google configuré côté Supabase + Google Cloud (redirect URL dev `localhost:3000/auth/callback`). Isolation RLS vérifiée en conditions réelles. **Confirmation e-mail : OFF en dev / ON en prod via 2 projets Supabase séparés (#17)** — le projet actuel = dev (mettre Confirm email OFF au dashboard).

- **#5 CRUD Clients** (mergé) : liste/création/fiche `app/(app)/clients/` sur **vraies données Prisma filtrées `where: { userId }`** (isolation vérifiée en réel). Server actions dans `app/(app)/clients/actions.ts` (`listClients`/`getClient`/`createClient`/`lookupSiret`). **SIRET** (colonne renommée `siren`→`siret`, 14 ch.) avec **validation live via l'API gratuite Recherche d'entreprises** (badge ✓ Vérifié + préremplissage nom/adresse) ; **recherche par nom d'entreprise** (combobox de suggestions) + re-autocomplete au changement de SIRET (#26). Formulaire **simplifié** (décision : pas de forme juridique/contact/CP-ville/taux TVA). Portée **Créer + Lire** : édition/suppression **faites (#58)**, agrégats CA/solde/dernier doc = placeholders jusqu'aux documents (#7). Badges nav sidebar restent mockés (24…) → à brancher.

- **#6 CRUD Projets** (mergé) : liste (vues grille + kanban, filtres) + modale « Nouveau projet » + fiche `app/(app)/projets/`. Server actions `app/(app)/projets/actions.ts` (`listProjects`/`getProject`/`createProject`/`listClientOptions`), filtrées `userId`, **`createProject` vérifie que le `clientId` appartient au user** (isolation vérifiée en réel). Hiérarchie user→client→**projet**→document. Formulaire simple (titre, client requis, description ; « Livraison estimée » non persistée). KPI financiers/documents = placeholders jusqu'à #8. Édition/suppression **faites (#58)**.

- **#8 Éditeur de document** (mergé) : `app/(app)/documents/nouveau` — éditeur 2 colonnes (formulaire + **aperçu A4 en temps réel**), lignes de prestation, **calcul TVA par ligne en centimes** live, numérotation à l'émission. Cœur métier pur et testé dans **`lib/invoicing/`** (money/numbering/legal/dates, **31 tests `bun test`** = #30 bien avancé). Server actions `app/(app)/documents/actions.ts` : `listProjectsForPicker`/`saveDraft`/`emitDocument` (numérotation transactionnelle FAC-/DEV-2026-NNN par user/type/an, franchise force 0 %, vérif appartenance projet). Émetteur A4 = profil user, **placeholders tant que #12 (Paramètres)**. PDF réel = #9 (bouton désactivé). Paywall 5/5 = #10 (à câbler avant `emitDocument`). Vérifié en réel (émission → FAC-2026-001, totaux centimes exacts).

- **#7 Listes Devis/Factures + vue Document** (mergé) : onglets `/factures` et `/devis` (bandeau synthèse, filtres par statut, table, **bouton « Nouvelle facture / Nouveau devis »** → éditeur) sur vraies données ; **vue document** `/factures/[id]` & `/devis/[id]` (A4 lecture + panneau statut/actions). Server actions `listDocuments`/`listInvoiceSummary`/`listQuoteSummary`/`getDocument`/`updateDocumentStatus` (statut `en_retard` dérivé, agrégats en BDD, filtré `userId`). Composants partagés `components/documents/` (document-list, document-paper, document-view, status-actions). « Marquer payé »/« Accepté/Refusé » fonctionnels ; PDF désactivé (#9). Émetteur = placeholders jusqu'à #12.

- **#9 PDF Puppeteer** (mergé) : route `app/api/documents/[id]/pdf/route.ts` (GET, runtime nodejs). Puppeteer NAVIGUE vers la vraie page `/factures/[id]` ou `/devis/[id]` (cookie de session forwardé) — une seule source de vérité visuelle, pas de HTML dupliqué. Dimensions figées `595×842px` (définition « A4 » du projet, pas le format A4 Puppeteer). Dépendances unifiées dev+prod : `puppeteer-core` + `@sparticuz/chromium` (WSL2 et Vercel sont tous deux Linux, pas de branchement dev/prod nécessaire). Sécurité : session (`getCurrentUser`, 401), appartenance (`getDocument`, 404), refuse les brouillons (400, pas de numéro légal). Téléchargement direct (pas de Supabase Storage). Boutons câblés dans `document-status-actions.tsx` et `document-list.tsx` (actifs seulement si le document est émis). CSS `print:` ajoutée sur le shell (Sidebar/Topbar/layout) et `document-view`/`document-paper` pour ne garder que le papier A4 au rendu PDF. **Bug corrigé au passage** : navigation liste Factures utilisait `/${type}/id` avec `type="facture"` (singulier) au lieu de `/factures/id` (pluriel) → 404 systématique au clic sur une ligne facture (masqué jusqu'ici car « devis » est invariant en français). Vérifié en réel : PDF valide généré (signature `%PDF`, rendu correct), tests E2E sécurité committés (`tests/e2e/documents-pdf.spec.ts` : 401/404/400/200 + isolation 2 users).

- **#10 Paywall freemium + Abonnement** (mergé) : forfait Gratuit limité à **5 documents émis/mois calendaire** (devis+factures confondus). Nouveau champ `Document.emittedAt` (horodatage SERVEUR, posé uniquement par `emitDocument()`) — jamais `issuedAt` qui est éditable par l'utilisateur et donc contournable. `emitDocument()` refuse la 6ᵉ émission (`app/(app)/documents/actions.ts`), garde de sécurité réelle. `getUsage()`/`upgradeToPremium()` dans `app/(app)/abonnement/actions.ts` (**upgrade simulé, sans paiement réel** — décision validée, aucun Stripe dans ce projet RNCP). Modale paywall accessible (`components/paywall/paywall-modal.tsx` : dialog, focus trap, Échap, `prefers-reduced-motion`) déclenchée par `GatedCreateLink` sur tous les boutons de création + garde-fou serveur sur l'accès direct à `/documents/nouveau` (redirige avec `?limite=1`, auto-ouvre la modale). Jauge sidebar réelle (rouge à 5/5, « Illimité » en Premium). Bannière d'avertissement sur `/factures` et `/devis`. Page `/abonnement` complète (`components/abonnement/subscription-view.tsx`) : jauges de conso, comparatif Gratuit/Premium avec toggle Mensuel(15€)/Annuel(12€/mois), upgrade réel en un clic (`router.refresh()`). **Bonus** : nouveau tiers de test `tests/integration/` (`.integration.ts`, exclu de `bun test` par défaut — lancé via `bun run test:integration` — écritures réseau réelles trop lentes pour la boucle rapide) + `tsconfig.json` exclut désormais `tests/` du typecheck `next build` (le checker intégré de Next skip nativement les `*.test.ts`/`*.spec.ts` mais pas les autres noms de fichiers sous `tests/`).

- **#11 Rapports d'activité** (mergé) : `app/(app)/rapports/actions.ts` (`getReportsData()`) — 4 KPI année en cours (CA encaissé HT + delta vs même période l'an dernier, en attente de paiement, délai moyen de paiement + delta, taux d'acceptation des devis **décidés** = accepté/(accepté+refusé)), graphique CA 12 mois (Jan→Déc, encaissé+en attente empilés, style identique au dashboard). **2 blocs Premium** (répartition CA par client top 4 + Autres, délais de paiement par client top 4 + Moyenne) : `null` pour un compte `free` — **le serveur ne les calcule ni ne les envoie**, décision de sécurité (le flou CSS de la maquette n'est pas une vraie barrière, un `null` empêche toute fuite via l'inspecteur). UI `components/rapports/` : cartes verrouillées avec contenu strictement décoratif (aucune vraie donnée) derrière le flou pour un compte free, vraies données sans flou en premium. Segment de période et « Exporter en PDF » désactivés (non implémentés, fidèles visuellement à la maquette). Vérifié en réel : zéro nom de client réel dans le HTML pour un compte free (recherche sur `page.content()`, pas juste visuel), données exactes recalculées à la main pour un compte premium.

- **#12 Paramètres** (mergé) : `app/(app)/parametres/actions.ts` (`getProfile()`/`updateProfile()`) — profil persisté sur les champs déjà modélisés en base (`name`, `activity`, `phone`, `address`, `siret`, `iban`, `bic`, `tvaRegime`) ; comme pour les Clients (#5), maquette `Profil.html` **simplifiée** (pas de nom commercial/forme juridique/TVA intracommunautaire/code APE/taux TVA par défaut/exigibilité/titulaire IBAN/format de numérotation éditable — non modélisés, hors AC). `updateProfile()` ne touche que les clés réellement fournies (chaque carte envoie ses propres champs, jamais d'écrasement croisé) ; validation SIRET (14 chiffres)/IBAN/BIC ; `nextDocumentNumber` = aperçu réel en lecture seule (réutilise `documentPrefix()`/`nextNumber()` de `lib/invoicing`, ne réserve rien). UI `components/parametres/` (sommaire sticky à ancres + scrollspy, 8 sections) : Identité/Légal/TVA (3 cartes radio)/Banque persistés et vérifiés par reload réel ; Préférences de facturation en lecture seule (rien à sauvegarder) ; Relances automatiques (Premium) **interactives côté client uniquement** (toggle/selects/ton/aperçu dynamique, bouton Enregistrer désactivé — aucune automatisation d'envoi n'existe dans ce projet, ne pas faire croire à une persistance) ; Apparence branchée sur le vrai mécanisme `localStorage['ff-theme']`/`data-theme` déjà en place (`app/layout.tsx`) ; Compte = vraie déconnexion (`signOut`). `lib/auth/initials.ts` extrait de `lib/auth/session.ts` (fonction pure réutilisable côté client).

- **#58 Édition/suppression Clients & Projets** (mergé) : boutons « Modifier » des fiches enfin actifs → modales pré-remplies (client : nom/SIRET/tél/e-mail/adresse/conditions ; projet : titre/statut/description — **clientId volontairement non modifiable**, les documents émis affichent ce client) + suppression avec confirmation 2 temps. Server actions `updateClient`/`deleteClient`/`updateProject`/`deleteProject` : pattern `updateMany`/`deleteMany` filtré `{ id, userId }` (0 ligne = « introuvable », même réponse que l'id existe chez autrui ou pas), **gardes RESTRICT** (client avec projets / projet avec documents → refus avec message clair, pré-check + rattrapage de contrainte). Nouveaux : `components/ui/modal-shell.tsx` (coquille de modale accessible réutilisable — les modales existantes ne sont pas migrées), variant `danger` du Button (tokens danger). Tests : `crud-edit-delete.integration.ts` (13 tests, isolation A/B avec instantané avant/après, preuve anti-régression filtre userId). Vérifié en réel (renommage en base, garde rendue dans la modale, Échap, sombre, zéro erreur console).

- **#56 Navigation instantanée** (mergé) : `vercel.json` `regions:["dub1"]` (fonctions co-localisées avec la BDD prod à Dublin — avant : `iad1` Washington, ~90 ms d'aller-retour par requête Prisma ; mesuré : login→dashboard 7,6 s → 2,2 s) + `app/(app)/loading.tsx` (squelette générique tokens, `role="status"`, `aria-hidden`, `motion-reduce` ; livré par le préfetch automatique des liens sidebar → feedback ~60 ms au clic, sidebar/topbar préservées, streaming ensuite). **Conséquence structurelle** : le streaming envoie le statut HTTP (200) avant `notFound()` → les PAGES ne renvoient plus de vrai 404 (l'API PDF, non streamée, si) ; les tests E2E d'isolation assertent le rendu (UI not-found + zéro fuite de données), pas le statut. Tests : `app/(app)/loading.test.tsx` (contrat a11y).

- **#47 Tableau de bord + badges sidebar sur vraies données** (mergé) : le dashboard (#1) et les badges de la sidebar avaient été créés avant la BDD et étaient restés **mockés** — désormais 100 % branchés sur Prisma. `app/(app)/dashboard/actions.ts` (`getDashboardData()`) : `todayLabel` (vraie date FR calculée serveur), 4 KPI du mois en cours (CA encaissé HT + delta % vs mois dernier, en attente, en retard, devis à relancer + potentiel), graphe CA 8 mois glissants, panneau « À traiter en priorité » (factures en retard puis devis à relancer, triés, jours réels), factures récentes, top clients du trimestre. `app/(app)/nav-counts.ts` (`getNavCounts()`) : comptes réels clients/projets/devis/factures pour la sidebar (chargés dans `app/(app)/layout.tsx` avec `getUsage()`, badge masqué à 0). **Convention HT/TTC** : agrégats stats en HT (comme #11), montants par document en TTC (comme les listes). `en_retard` dérivé (jamais stocké). **`components/dashboard/mock-data.ts` supprimé** (type `TagTone` déplacé dans `tag.tsx`). États vides propres (compte neuf → zéros + messages sobres, `topClients: null`). Vérifié en réel : compte rempli (valeurs exactes recalculées à la main, badges 5/5/3/8) + compte vierge (aucune fuite d'ancien mock : « 8 420 », « Camille & Co », date figée absents du HTML).

- **#60 Écran « Premier lancement »** (mergé) : **les 21 maquettes hifi sont désormais toutes implémentées.** Un compte sans AUCUN document voit, à la place du tableau de bord, le guide « Premiers pas » (maquette `Premier lancement.html`) : checklist 3 étapes câblées sur le réel (① SIRET/régime TVA → /parametres #12, ② premier client → /clients/nouveau #5, ③ premier document → /documents/nouveau #8), états done/courante calculés depuis la base, carte d'aide, KPI vides. Logique pure dans `lib/onboarding.ts` (`computeOnboarding()`, tests unitaires) ; `getOnboardingStatus()`/`dismissOnboarding()`/`isOnboardingDismissed()` dans `app/(app)/dashboard/actions.ts` (3 counts légers filtrés `userId`, lancés en parallèle du fetch dashboard — zéro aller-retour séquentiel ajouté, cf. #56). « Ignorer ce guide » = cookie `ff-onboarding-dismissed` (1 an, httpOnly, per-appareil — compromis assumé, l'écran disparaît partout au 1er document). UI `components/onboarding/` (vue serveur + îlot client dismiss). Écarts maquette assumés : SIREN→SIRET (renommage #5), mois réel dans le KPI, bouton « Ignorer » ajouté (exigence issue). Vérifié en réel (22 assertions navigateur : 0/3, 2/3 avec coches, dismiss+reload, clavier, 2 thèmes) ; tests : 8 unit + 4 intégration (isolation A/B) + 2 E2E.

- **#62 Audit éco-conception EcoIndex** (mergé) : exigence « EcoIndex ≥ B » du cahier des charges **mesurée et dépassée — grade A sur les 12 pages clés** (pire : /dashboard 81/100, le B commence à 70). Rapport complet dans **`docs/ecoindex.md`**. Outillage committé dans `scripts/eco/` : `ecoindex.ts` (formule officielle cnumr/GreenIT-Analysis, pure, testée), `audit.ts` (audit navigateur rejouable : 12 pages à froid, compte éphémère semé sur Supabase dev, exit 1 si < B), `check-size-budget.ts` + `size-budget.json` (**garde-fou CI dans le job build** : JS gzippé de premier chargement par route via `app-build-manifest.json`, 130 Ko défaut, dérogation 190 Ko `/connexion`/`/inscription` — client Supabase requis par l'OAuth Google). ⚠️ `scripts/` est typechecké par `next build` (target es5 : pas de spread de Set/Map). Toute nouvelle dérogation de budget : justifier dans `docs/ecoindex.md`.

- **#61 Conversion devis → facture** (mergé) : sur un devis **accepté**, « Convertir en facture » crée un **brouillon** de facture (projet/objet/totaux/lignes **copiés à l'identique en centimes**) puis ouvre l'**éditeur pré-rempli** dessus — numérotation FAC- et quota freemium restent du ressort d'`emitDocument()`. Traçabilité : colonne `documents.source_quote_id` (migration `20260714090000`, auto-référence SET NULL, **index UNIQUE** = un devis → une seule facture, ferme la course double-conversion, rattrapage P2002) — **appliquée sur dev ET prod**. Relation Prisma 1-1 `QuoteConversion` (`sourceQuote`/`convertedInvoice`, exposés par `getDocument`). Nouveau : `getDraftForEditor()` + **`/documents/nouveau?document=<id>`** recharge n'importe quel brouillon dans l'éditeur (bouton « Reprendre dans l'éditeur » sur la vue d'un brouillon — avant, un brouillon n'était ni modifiable ni émissible). Panneau devis : bouton actif seulement si accepté, devient lien « Voir le brouillon / la facture FAC-… » après conversion ; note « Créée à partir du devis » sur la facture. Tests : 12 intégration (copie centimes exhaustive prouvée par mutation, anti-double, chaîne conversion→émission, isolation A/B) + 2 E2E ; vérifié en réel 16/16 assertions navigateur (2 thèmes, zéro erreur console).

- **#63 Recherche globale ⌘K** (mergé) : le champ de la topbar (décoratif depuis le shell) est branché. `app/(app)/search.ts` (`searchAll()`) : 3 requêtes parallèles filtrées `userId` — clients par nom, projets par titre, documents par **numéro** (brouillons sans numéro exclus) — `contains` insensible à la casse, `take: 5`, zod 2–80 caractères. `components/layout/search-palette.tsx` : palette sur ModalShell (clic ou ⌘K/Ctrl+K), combobox ARIA complet (`aria-activedescendant`, listbox/option), navigation ↑ ↓ ↵ trans-groupes, debounce 250 ms + rejet des réponses périmées. **Fix ModalShell au passage : rendu via `createPortal(document.body)`** — le `backdrop-blur` de la topbar crée un containing block CSS qui rognait toute modale `fixed` rendue dedans (piège à retenir pour tout composant fixed sous la topbar). tsconfig exclut `**/*.tmp.ts` (scripts de vérif jetables à la racine — typecheckés par `next build` sinon). ⚠️ Sélecteurs E2E : la topbar a désormais un bouton dont le nom accessible contient « Rechercher » — utiliser `exact: true` pour viser d'autres boutons « Rechercher ». Tests : 13 intégration (isolation A/B prouvée par mutation) + 2 E2E ; vérifié en réel 16/16 assertions (2 thèmes, zéro erreur console).

- **#64 Export PDF des rapports** (mergé) : bouton « Exporter en PDF » de /rapports branché sur **`GET /api/rapports/pdf`** — même architecture que le PDF document (#9) : session requise (401 JSON), Puppeteer navigue vers la vraie page `/rapports` avec cookie forwardé (zéro HTML dupliqué), mais **format A4 standard multi-pages** (pas le 595×842 figé des documents). **Règle Premium par construction** : la page est rendue pour la session → un compte free reçoit `null` (#11) → le PDF ne peut pas contenir plus que l'écran (vérifié sur PDF binaires réels free + premium). Print : outils d'en-tête `print:hidden`, `print:break-inside-avoid` sur chaque carte. Tests : `tests/e2e/rapports-pdf.spec.ts` (401 JSON / 200 + `%PDF` + content-disposition / bouton actif).

- **#65 Segments de période réels** (mergé) : les segments du dashboard (« Ce mois / Trimestre / Année ») et des rapports (« Année N / 12 derniers mois / Trimestre »), décoratifs depuis #1/#11, pilotent les données. Sélection dans l'**URL** (`?periode=`, whitelist stricte, défaut = URL sans paramètre, `router.replace` + `aria-pressed`). **`lib/periods.ts`** (pur, 29 tests) : bornes UTC semi-ouvertes + période de comparaison — dashboard : période calendaire courante vs précédente **complète** ; rapports : année **à date** vs N-1 (convention #11) / 12 mois glissants / trimestre. `getDashboardData(periode)` : KPI 1 toujours **dérivé des seaux du graphe** (fenêtre élargie jusqu'à 24 seaux en année, graphe = 8 derniers, **zéro requête ajoutée**) ; top clients sur la période ; KPI 2/3/4 invariants (états instantanés, testé). `getReportsData(periode)` : toutes bornes paramétrées (graphe 12 ou 3 seaux, blocs Premium compris) + libellés serveur. L'export PDF (#64) emporte la période. Tests : 29 unit + 9 intégration (attendus recalculés via les fenêtres — stables toute l'année ; isolation A/B ×6) + 2 E2E ; vérifié en réel 14/14 (mois 800 €/+100 %, année 1 200 €/+33 %, 2 thèmes, zéro erreur console).

- **#66 Dupliquer un document** (mergé) : `duplicateDocument()` — filtrée `userId`, duplique tout document (devis/facture, émis ou brouillon) en **nouveau brouillon du même type** (projet/objet/totaux/lignes copiés à l'identique en centimes), sans numéro ni `emittedAt` (**quota freemium intact**) ni `sourceQuoteId` (≠ conversion #61), duplication illimitée (récurrence). Bouton « Dupliquer » actif dans le panneau vue document + **bouton par ligne** dans /factures /devis (aria-label « Dupliquer FAC-… », erreur `role=alert`) → éditeur pré-rempli (`?document=`, infra #61). Tests : 8 intégration (copie prouvée par mutation ±1 centime, original intact, quota inchangé, isolation A/B, chaîne duplication→émission) + 2 E2E ; vérifié en réel 14/14. ⚠️ Sélecteurs E2E : une ligne de liste « brouillon » affiche « Brouillon » DEUX fois (cellule pièce + tag) — compter les `row`, pas les `getByText`.

- **#68 Compte : mot de passe / e-mail / suppression RGPD** (mergé) : la section Compte des Paramètres n'offrait que la déconnexion. `app/(app)/parametres/account-actions.ts` (3 server actions, `requireUserId` + e-mail lu sur `public.users` — homogène avec tout le projet, **jamais `getCurrentUser`** : son import nommé était lié sous le mock partiel `@/lib/auth/session` d'un autre fichier d'intégration — les `mock.module` de Bun fuient dans un même process « dernière inscription gagne ») : `changePassword` (re-vérifie le mot de passe ACTUEL via un client Supabase jetable `persistSession:false` avant `updateUser`), `changeEmail` (`updateUser({email})` → double confirmation Supabase ; trigger `on_auth_user_email_updated` migration `20260715090000` répercute sur `public.users`, **appliquée dev+prod**), `deleteAccount` (droit à l'effacement : confirmation forte = saisie exacte de l'e-mail, transaction Prisma enfants→parents, `admin.deleteUser`, signOut, redirect `/`). UI `components/parametres/account-card.tsx` (4 blocs + modale ModalShell, bouton désactivé tant que l'adresse ≠ compte). Tests : 9 intégration (cascade RGPD complète + isolation A/B par instantané, confirmation forte, validation, trigger e-mail) + 2 E2E (bascule mot de passe réelle, suppression + reconnexion impossible). ⚠️ Rappel : une server action ne doit dépendre que de `requireUserId` (jamais `getCurrentUser`) pour rester mockable en intégration sans casser les autres fichiers.

- **#67 Pages d'erreur personnalisées (not-found + error) en français** (mergé) : la 404 était celle de Next par défaut (« This page could not be found », anglais — d'autant plus visible depuis #56), et aucune `error.tsx` n'existait (exceptions serveur → écran générique anglais). Trois fichiers + une présentation mutualisée : `app/not-found.tsx` (404 racine hors shell, URL publiques inconnues, lien « Retour à l'accueil » — rappel middleware : un chemin de premier niveau inconnu n'est pas dans `PROTECTED_PREFIXES` → pas de redirection, il rend bien cette page) ; `app/(app)/not-found.tsx` (404 **dans** le shell sidebar/topbar — l'écran vu quand une fiche est inexistante/pas au compte : filtre `where userId` → `notFound()`, #56, sans fuite) ; `app/(app)/error.tsx` (**composant client** obligatoire, contrat error.tsx de Next : message FR + bouton « Réessayer » = `reset()` + retour dashboard ; erreur `console.error` mais jamais affichée) ; `components/errors/error-state.tsx` (présentation **sans hook ni "use client"** → importable serveur ET client, tokens only, thèmes auto, a11y : titre `<h1>`, code décoratif `aria-hidden`). ⚠️ La 404 racine renvoie un **vrai statut HTTP 404** (non streamée, ≠ pages du groupe app depuis #56) → le navigateur journalise le chargement de ressource comme « erreur » : bruit d'infra à filtrer en E2E (`/Failed to load resource|404/`). Tests : `tests/e2e/error-pages.spec.ts` (404 racine française + absence de « could not be found ») ; les 2 tests d'isolation A/B de `clients-projets.spec.ts` assertent désormais « introuvable » (au lieu du regex bilingue). Vérifié en réel (4 captures : racine + shell × 2 thèmes, zéro erreur console applicative).

- **#69 Notifications réelles (cloche topbar)** (mergé) : la cloche était décorative. `app/(app)/notifications.ts` (`getNotifications()` + `markNotificationsRead()`) — notifications **dérivées des mêmes clauses `where` que le panneau « À traiter » du dashboard** (#47), aucune table ni statut stocké : factures en retard (`facture` `envoye` non payée, `dueAt < now`) + devis sans réponse depuis > 7 j (`devis` `envoye`, `emittedAt ?? issuedAt` avant le seuil). 2 `findMany` bornés (`take 12`, `where userId`, parallèles). **« Lu / non lu » par cookie `ff-notifs-read-at`** (ms epoch, per-appareil, comme #60 — **aucune migration**) : un item est non lu si son horodatage d'entrée dans le set (`dueAt` pour une facture, `emittedAt + 7 j` pour un devis) est postérieur au dernier « tout marquer comme lu ». **Éco** : chargé dans le `Promise.all` **existant** de `app/(app)/layout.tsx` (à côté de `getUsage`/`getNavCounts`) → zéro aller-retour séquentiel ajouté par navigation (cf. #56). UI `components/layout/notifications-bell.tsx` : cloche + badge de compte (« 9+ » au-delà) + popover accessible (`aria-haspopup="dialog"`/`aria-expanded`, panneau `role="dialog"` étiqueté, Échap rend le focus, clic extérieur ferme) ; « Tout marquer comme lu » = server action + `router.refresh()` (badge recalculé serveur). ⚠️ Le panneau est en **`position:absolute`** (le `backdrop-blur` de la topbar est un containing block pour `position:fixed` — même piège qu'en #63, mais l'absolu s'y soustrait). `Topbar` prend désormais une prop `notifications` (plus de rendu sans argument). Tests : 4 intégration (isolation A/B, exclusion des leurres payé/non-échu/accepté/brouillon/devis récent, calcul lu/non lu, `markNotificationsRead` ; **cookies mockés via `mock.module("next/headers")`** — magasin en mémoire) + 1 E2E (vide → semis → badge → liste → « tout lu » efface le badge). Vérifié en réel 2 thèmes, zéro erreur console.

- **#70 Listes : tri réel + pagination serveur (éco-conception)** (mergé) : les 4 listes chargeaient TOUT sans pagination et le chip « Trier » des projets était décoratif. **Socle** `lib/pagination.ts` (pur, 11 tests) : `PAGE_SIZE` (clients/documents 10, projects 12), `parsePage` (clamp des `?page=` forgés → jamais d'erreur/skip négatif), `paginate` → `{page, skip, totalPages, total}`, type `Paginated<T> = { items, pagination }`. `components/ui/pagination.tsx` : `<nav aria-label="Pagination">`, liens Précédent/Suivant **réels** (préservent les autres params, bornes = `<span aria-disabled>` non focusable). **État dans l'URL** partout (`?page=`, `?statut=`, `?tri=`). **Clients** : `listClients(page)`. **Documents** : `listDocuments(type, {page,status})` + `getDocumentCounts(type)` (compteurs par statut **effectif** — `en_retard` dérivé — via `count()` indexés + dernière pièce émise) ; **`statusFilterWhere()` = source unique** partagée liste/compteurs (chips = liens `?statut=`, comptes réels tous statuts, plus dérivés d'`items`). **Projets** : `listProjects({page,status,sort})`, **tri réel** (`recent`/`nom`) via un `<select>` `?tri=` remplaçant le chip décoratif, `getProjectCounts()` (`groupBy`) nourrit chips grille **et** badges Kanban (totaux réels) ; vue grille/kanban reste locale, le Kanban groupe la page courante. Sidebar **inchangée** (`getNavCounts` dédié). ⚠️ **Rupture d'API** : `listClients`/`listProjects`/`listDocuments` renvoient maintenant `{ items, pagination }` (avant : tableau) → penser à `.items` (a cassé 4 tests d'isolation existants au 1er passage, corrigés). ⚠️ **`DOCUMENT_FILTERS` n'est PAS exporté** (fichier `"use server"` = seules des fonctions async exportables). Tests : 11 unit (bornes/clamp) + 3 intégration (première/dernière page, page hors limites ramenée, compteurs par statut effectif, isolation A/B) + 1 E2E (état URL page/statut, exclusion du brouillon sous filtre). Vérifié en réel sur les 4 listes, 2 thèmes, zéro erreur console.

## Évolutions post-MVP (roadmap #82-88)

Après le backlog #60-70, feuille de route d'évolutions validée (« go je te laisse tout gérer »). Issues créées le 2026-07-16. Clés externes fournies par le mainteneur le 2026-07-17 (Stripe test, Resend, Sentry — **stockées `.env.local` + Vercel prod/preview uniquement, jamais commitées, jamais affichées en chat**) : #82/#83/#88 débloquées.

- **#86 Agrégats financiers réels sur les fiches client & projet** (mergé) : les fiches client (CA, en attente, pièces, délai) et projet (facturé, budget, encaissé, reste) affichaient des placeholders (0/—) alors que les documents existent (#7/#8). `getClientFiche(id)` (clients/actions.ts) : profil + stats **HT** (CA année civile, en attente, pièces émises `number != null`, délai moyen de paiement calculé sur factures payées) + projets rattachés + historique des pièces (bornés `take 10`) — UNE vérif d'appartenance puis lectures **parallèles** filtrées via `project.clientId`. `getProject` enrichi (projets/actions.ts) : **budget = Σ HT des devis ACCEPTÉS** du projet, facturé = Σ HT factures émises, encaissé/en attente, **reste à facturer = budget − facturé**, `documentsCount` + liste (onglet Documents, `take 20`). UI : `clients/[id]/page.tsx` (tuiles + Projets + Historique), `projets/[id]/page.tsx` + `project-tabs.tsx` (bandeau + onglet Documents). **Conventions #47** : agrégats HT, montants par pièce TTC, « en_retard » dérivé. Écart assumé : budget hors devis accepté & « phases de mission » non modélisés (« — »). ⚠️ `getProject` renvoie désormais des champs financiers (avant : stubs `0`) mais `ProjectDetail` garde `name/status/notes/clientId` → les tests existants qui ne lisent que ces champs restent verts. Tests : 3 intégration (valeurs recalculées + isolation A/B) ; vérifié en réel (2 fiches, 2 thèmes, zéro erreur console).
- **#85 Lien public de devis (accept/refus sans compte)** (mergé) : un devis émis se partage par URL tokenisée `/proposition/<token>` ; le client l'accepte/refuse **sans compte**. Migration `documents.public_token` (text unique nullable) — **dev + prod**. `documents/actions.ts` : SELECT + mapper `toDocumentView` **mutualisés** (getDocument authentifié / `getPublicQuote` public `where { publicToken, type:'devis' }`, jamais une facture) ; `respondToQuote(token, accept|refuse)` = transition **atomique** `updateMany` depuis « envoye » seulement (0 ligne = déjà tranché/révoqué) ; `createShareLink`/`revokeShareLink` propriétaire seul (`where userId`, devis émis, jeton `randomBytes(24).base64url`, idempotent). Route **publique** `app/(public)/proposition/[token]/page.tsx` — hors shell **et hors middleware auth** (le chemin n'est pas dans `PROTECTED_PREFIXES` ⇒ pas besoin de toucher au middleware ; ⚠️ ne jamais mettre la route publique sous `/devis/...` qui EST protégé), rendu A4 lecture seule (`DocumentPaper` partagé), `robots:noindex`, jeton invalide → 404 (#67). UI : `components/public/quote-response.tsx` + `components/documents/share-quote.tsx` (dans la vue devis émis). Sécurité : le jeton EST le secret ; la mutation publique ne fait que `envoye→accepte/refuse`. Tests : 5 intégration (leurres brouillon/facture, idempotence, jeton valide/invalide/révoqué, double réponse bloquée, isolation A/B) + 3 E2E. ⚠️ `DocumentView` porte désormais `publicToken`.
- **#88 Monitoring des erreurs de production (Sentry)** (mergé) : `app/(app)/error.tsx` (#67) ne faisait que `console.error`. `@sentry/nextjs` (palier Developer gratuit) : `sentry.client/server/edge.config.ts` (DSN en variable **publique** — ce n'est pas un secret, il ne permet que d'ÉMETTRE des événements) ; `instrumentation.ts` (`register()` par runtime + `onRequestError` pour le rendu serveur App Router — nécessite `experimental.instrumentationHook: true` sur Next 14, stable sans flag depuis Next 15) ; `next.config.mjs` enveloppé par `withSentryConfig`. `Sentry.captureException()` ajouté dans `app/(app)/error.tsx` ; nouveau `app/global-error.tsx` (seul filet pour une exception dans le layout RACINE — doit fournir son propre `<html>/<body>`, contrat Next.js). **PII** : `beforeSend()` retire `request.data`/`cookies` (server+edge) avant envoi. **Pas de tracing ni Session Replay** (hors périmètre « erreurs ») — ces options gonflent le bundle client de ~30 Ko gzip même à 0, `webpack.treeshake.removeTracing`/`removeDebugLogging` activés pour ne garder que la capture. ⚠️ **Dérogation budget éco-conception** (justifiée dans `docs/ecoindex.md`, procédure #62) : le SDK ajoute ~10 Ko gzip incompressibles par route authentifiée → `size-budget.json` relevé (130→150 Ko défaut, 190→210 Ko connexion/inscription). Build vert **avec et sans** `NEXT_PUBLIC_SENTRY_DSN` (CI n'injecte pas ce secret, aucun besoin — `Sentry.init({dsn: undefined})` no-op proprement). Vérifié en réel : route de debug temporaire (non committée) → `error.tsx` français **et** 8 requêtes réseau confirmées vers l'hôte d'ingestion exact du DSN (pas de token API Sentry pour lire le dashboard, donc preuve par observation réseau).
- **#83 Envoi du devis / de la facture par e-mail au client** (mergé) : le statut « envoyé » était purement déclaratif (#7/#8) — aucun e-mail réel. `lib/pdf.ts` : rendu Puppeteer **extrait** de la route de téléchargement (#9, refactor pur, route inchangée) pour être réutilisé par l'envoi e-mail — mêmes octets, une seule source de vérité. Migration `documents.email_sent_at` (timestamptz nullable, horodatage serveur, dev+prod) — distincte du `status` métier. `documents/email-actions.ts` : `sendDocumentByEmail(id,{to})` — session + appartenance (`getDocument`), refuse un brouillon, valide l'adresse (zod), PDF généré puis envoyé via **Resend** (from `onboarding@resend.dev`, reply-to = e-mail du freelance), persiste `emailSentAt`. **Architecture en deux temps** : `sendDocumentByEmail` (résout origin/cookie via `next/headers`) délègue à `sendDocumentByEmailCore` (logique pure, testable) — évite le conflit avec le mock `next/headers` incompatible de `notifications.integration.ts` (cf. #68) sans toucher aux tests existants. UI `send-email.tsx` sur **tout document émis** (devis ET facture). ⚠️ **Palier Resend gratuit sans domaine vérifié** : seule l'adresse du compte Resend peut recevoir un envoi (anti-abus) — erreur détectée (`/own email address|verify a domain/i`) et traduite en français ; `@example.com` déclenche un AUTRE garde-fou Resend (domaine placeholder, message différent, non traduit). Éco : Resend importé serveur uniquement, zéro impact bundle client. Tests : 6 intégration (PDF/Resend mockés, isolation A/B, payload Resend asserté, persistance) + **2 E2E contre la vraie API Resend** (secret CI `TEST_RESEND_API_KEY` ajouté — destinataire de test toujours rejeté par la restriction anti-abus, zéro e-mail réellement délivré en CI, zéro risque de quota). **Vérifié en réel hors CI** : envoi effectif jusqu'à réception d'un e-mail avec PDF joint, `emailSentAt` persistant après reload, 2 thèmes, zéro erreur console.
- **#82 Paiement Premium via Stripe (mode TEST — aucun argent réel)** (mergé) : l'upgrade « simulé » de #10 est remplacé par un vrai parcours Stripe Checkout en mode test (cartes 4242 uniquement — le site est une démo RNCP, contrainte « 100 % gratuit » du mainteneur). **Décision sécurité clé : `upgradeToPremium()` SUPPRIMÉE** (une server action publique qui accorde le Premium devenait une faille dès qu'un paiement existe) — le forfait n'est accordé/retiré QUE par le webhook signé. `lib/stripe.ts` : `getStripe()` **paresseux obligatoire** (`new Stripe()` lève sans clé au chargement du module → planterait la collecte de pages de `next build` en CI sans secrets ; ≠ Sentry qui no-op). `lib/premium.ts` : `applyPremiumPlan()`/`revokePremiumPlan()` — volontairement PAS `"use server"` (non appelables du navigateur). `abonnement/actions.ts` : `createCheckoutSession(cycle)` → wrapper/Core (pattern #68/#83) — 1 customer Stripe par user (réutilisé, `metadata.userId`), session `mode:subscription`, `client_reference_id=userId`, retour `?checkout=success|cancel`. `app/api/stripe/webhook/route.ts` (POST, nodejs, **corps BRUT** `request.text()` avant `constructEventAsync` — jamais `.json()`, la signature HMAC porte sur les octets exacts ; ⚠️ variantes **Async** obligatoires sous Bun, les sync lèvent `SubtleCryptoProvider`) : `checkout.session.completed` → apply ; `customer.subscription.deleted`/`invoice.payment_failed` (sub via `invoice.parent.subscription_details`) → revoke par lookup `stripeSubscriptionId` ; idempotent par construction. Migration `20260717120000` (`stripe_customer_id`/`stripe_subscription_id` + index uniques) — **dev + prod**. UI : redirection réelle vers Checkout, bannières success/cancel, refresh différé 2 s (course webhook). CI : secrets `TEST_STRIPE_*` injectés dans les jobs de test (skip propre si absents), le job build reste SANS secrets (d'où le lazy). Tests : 8 intégration webhook (signatures HMAC réelles `generateTestHeaderStringAsync`, 400 réels, grant/revoke/idempotence) + 3 intégration checkout (vraie API test, cleanup) + 1 E2E (navigation réelle checkout.stripe.com). **Vérifié en réel EN PRODUCTION** : compte jetable → Checkout carte 4242 → retour success → webhook livré → `plan_type=premium` + `stripe_subscription_id` en base prod → UI Premium ; customer Stripe et compte de test supprimés après coup. **Bonus anti-flaky CI** : `retries: 2` Playwright en CI uniquement (course d'hydratation React sur runner lent apparue avec le JS Sentry #88 — clic avant handler attaché, test différent à chaque run ; fix ciblé re-clic dans duplicate-document + retries systémiques ; 0 retry en local).
- **#84 Relances automatiques des factures en retard (Vercel Cron)** (mergé) : la carte « Relances automatiques » des Paramètres (#12), décorative, est réelle de bout en bout. **Réglages persistés** (`users.reminders_enabled`/`reminder_first_days`/`reminder_second_days`/`reminder_final_days`/`reminder_tone`, migration `20260718100000` **dev + prod**) via `updateReminderSettings()` (whitelist zod stricte = valeurs des selects de la maquette) ; un compte free peut sauvegarder (copie existante « s'activera dès votre passage en Premium »), **le cron ne traite que premium + activé**. **`lib/reminders.ts`** (pur, 12 tests) : paliers J+N, **idempotence quotidienne** (jamais 2 relances le même jour UTC, rattrapage progressif 1/jour), e-mails par ton (textes des aperçus de la carte, L441-10 en ton ferme, TTC + échéance réels — ⚠️ espace insécable U+202F dans le format fr-FR, asserter via `formatEuros`). **`lib/reminder-sweep.ts`** (PAS `"use server"`, comme lib/premium.ts — il écrit pour n'importe quel user éligible) : sélection = mêmes clauses que la dérivation « en retard » (#47/#69), bornage SQL au 1er palier, envoi **Resend sans PDF** (un cron n'a pas de session → le rendu Puppeteer de lib/pdf est inutilisable par construction), trace `documents.reminder_count`/`last_reminder_at` **seulement si l'envoi réussit** (échec retenté), journalisation JSON par run. **Route `GET /api/cron/relances`** : `Authorization: Bearer ${CRON_SECRET}` (envoyé nativement par Vercel Cron quand la variable existe — posée Vercel prod+preview et `.env.local`), 401 sinon ; `vercel.json` `crons` quotidien 8h UTC (plan Hobby : 1×/jour max, horaire non garanti à la minute). Vérifié en réel : e-mail de relance effectivement reçu (sweep réel `sent:1`, trace persistée), cron enregistré sur le déploiement prod (API Vercel), route prod 401 sans secret, carte 2 thèmes zéro erreur console. Tests : 12 unit + 7 intégration (6 leurres, idempotence, rattrapage — un leurre « échu récent » DEVIENT éligible quand `now` avance, asserter par facture — échec Resend sans trace, whitelist, route 401/200) + 2 E2E.
- **#87 Logo d'entreprise sur les documents (Supabase Storage)** (mergé) : le bouton « Ajouter votre logo » de la carte Identité (#12, décoratif) est réel — **le Storage est enfin utilisé**. Bucket **PRIVÉ** `logos` (migration `20260718130000` **dev + prod**) : 2 Mo max + MIME whitelistés (PNG/SVG/JPEG) **au niveau du bucket**, doublés par la validation de l'action ; **RLS storage.objects** = 4 policies `(storage.foldername(name))[1] = auth.uid()`. **Écritures via le client de SESSION** (`lib/supabase/server`) — la RLS est la vraie barrière, jamais la clé service pour écrire. **Affichage par URL SIGNÉE** (`lib/logo.ts` `signedLogoUrl()`, TTL 1 h, clé service en LECTURE seule sur un chemin dont l'appartenance est établie par la requête appelante) : `getProfile` + `getDocument`/`getPublicQuote` (`emitter.logoUrl` sur `DocumentView`) → en-tête A4 partout (vue, page publique /proposition, **PDF #9 sans aucun code ajouté** — Puppeteer navigue vers la page qui embarque l'URL signée). **Garde serveur Premium sur l'upload** (cohérence /abonnement : « Logo personnalisé sur les documents » = Premium) ; suppression ouverte à tous (compte redescendu free) ; remplacement supprime l'ancien objet si l'extension change (l'upsert ne l'écraserait pas) ; fallback = avatar initiales / en-tête texte inchangés. `logo-actions.ts` (`uploadLogo(FormData)`/`deleteLogo`) ; ⚠️ un composant client ne voit pas un `router.refresh()` invalider son état : la carte garde l'aperçu en `URL.createObjectURL` local après upload. Tests : 6 intégration **contre le VRAI Storage** (B refusé écriture/lecture/signature chez A, anonyme refusé, URL signée lisible publiquement, MIME refusé par le bucket, garde Premium + validations — les cas d'action testés répondent AVANT le client next/headers, happy path couvert en E2E) + 2 E2E (free désactivé ; upload PNG réel → A4 `src*=/object/sign/` → suppression → fallback). Vérifié en réel : logo visible sur la vue facture ET dans le PDF Puppeteer téléchargé (contrôle visuel du binaire), 2 thèmes, zéro erreur console.

**🎉 Backlog #82-88 terminé** : plus aucune issue ouverte de la roadmap post-MVP.

- **#96 Version responsive (mobile)** (mergé) : l'app était desktop-only — audit réel iPhone 13 (390 px) : débordement horizontal sur TOUTES les pages (+358 à +493 px), sidebar 248 px sur 2/3 de l'écran. **`components/layout/app-shell.tsx`** (client) : desktop ≥ `lg` STRICTEMENT inchangé (grille sidebar+contenu) ; mobile = sidebar en **tiroir off-canvas** — bouton menu 44 px dans la topbar (`aria-expanded`/`aria-controls`), fond cliquable, Échap ferme + rend le focus, fermeture auto au changement de route, **`invisible` à l'état fermé** (⚠️ un translate hors écran ne retire NI du focus clavier NI des lecteurs d'écran — piège a11y), transition `[transform,visibility]` pour garder l'animation de sortie, filet `overflow-x-clip` sur la racine (clip sans conteneur de scroll, sticky préservé). Topbar : menu < lg, titre truncate, déclencheur recherche icône seule < sm (aria-label = nom accessible stable pour les sélecteurs E2E). **Balayage additif** (~15 composants, zéro changement desktop — le wrap ne se déclenche que faute de place) : barres d'actions d'en-tête + chips repliables (`flex-wrap`), table dashboard et graphe 12 mois rapports scrollables DANS leur carte, rangées d'historique des fiches repliables, nav publique compacte (ancres + texte de marque masqués mobile), CTA hero repliables, contenu `p-4 sm:p-7`. Résultat : **zéro débordement sur les 18 pages** à 390 px. Tests : `tests/e2e/responsive.spec.ts` — ① boucle anti-débordement sur 17 pages avec données réelles (`scrollWidth ≤ clientWidth`, tout nouveau débordement casse la CI), ② parcours tiroir complet. ⚠️ Budgets CI : 17 chargements sur runner lent → `test.setTimeout(240s)`, `networkidle` borné 8 s avec repli + settle 300 ms, assertions post-navigation 15 s (streaming). Vérifié en réel : parcours mobile complet + captures desktop 1440 identiques, zéro erreur console.

### Mocks restants à brancher plus tard
- Jauge freemium : **branchée (#10)** via `getUsage()` — cette ligne est obsolète, plus aucun mock de jauge.
- ~~Badges nav sidebar~~ → **fait (#47)** : vrais comptes via `getNavCounts()`.
- ~~User-chip « Camille Laurent » + date de salutation~~ → **fait (#21 puis #47)** : vrai utilisateur (nom/e-mail, `components/layout/user-menu.tsx`) + vraie date du jour (`todayLabel`). Plus aucun mock sur le tableau de bord.

## Workflow Git

Pour toute fonctionnalité/correctif, utiliser le skill **`/issue-flow <description>`** (`.claude/skills/issue-flow/SKILL.md`) : il crée l'issue GitHub, la branche liée (`feat/<num>-<slug>`), implémente, vérifie le build, ouvre la PR (`Closes #<num>`) et merge en squash. Repo : `Aure33/FreelanceFlow`, `gh` CLI authentifié.

## Équipe d'agents (`.claude/agents/`)

| Agent | Domaine | Quand |
|---|---|---|
| `frontend-ui` | Écrans/composants pixel-perfect depuis les maquettes, tokens, a11y | Toute UI |
| `backend-data` | Prisma, Supabase, RLS, auth, server actions, requêtes | Toute donnée/sécurité |
| `invoicing-pdf` | Calculs TVA (centimes !), régimes/mentions légales, numérotation, A4, PDF Puppeteer | Tout le métier facturation |
| `test-author` | Écrit les tests **committés** : unitaire `bun test` (métier/centimes), intégration server actions + isolation `userId`/RLS, E2E Playwright versionnés `tests/e2e/*.spec.ts` | Pendant chaque issue, après l'implémentation, avant `qa-verifier`/PR |
| `qa-verifier` | Vérification critique (Playwright, maquettes, a11y, sécu, éco) — rapporte, ne corrige pas | Après chaque feature, avant PR |

L'agent principal orchestre, garde la structure du projet et le workflow Git (issues/PR/merge).

## Vérification navigateur

Skill **`/playwright-verify [route]`** (`.claude/skills/playwright-verify/SKILL.md`) : teste chaque feature dans Chromium headless (playwright installé en devDependency) — screenshots clair/sombre comparés aux maquettes, interactions, erreurs console, port dédié 3199. Étape obligatoire d'issue-flow avant PR. Si Chromium ne se lance pas (libs système WSL manquantes) : `sudo ~/.bun/bin/bunx playwright install-deps chromium`.

## Conventions

- **Langue** : UI et commentaires en français (copies exactes des maquettes)
- **Tokens uniquement** : jamais de couleur en dur — utiliser les classes mappées (`bg-surface`, `text-ink-2`, `border-line`, `bg-accent-soft`, `text-accent-ink`, `bg-ok-soft`…). Thème sombre automatique via les variables CSS
- **Classes layout dispo** : `h-topbar` (64px), `w-sidebar`/`grid-cols-[var(--sidebar-w)_1fr]` (248px), `p-pad` (22px), `gap-gap` (20px), `max-w-content` (1320px), padding contenu = `p-7` (28px)
- **Rayons** : `rounded-sm`=7px, `rounded-md`=10px, `rounded-lg`=14px, `rounded-xl`=20px (écrasent les valeurs Tailwind par défaut)
- **Montants/compteurs** : classe utilitaire `.num` (JetBrains Mono + tabular-nums)
- **Icônes** : lucide-react, `strokeWidth={1.9}` dans la nav, 2 ailleurs (cf. maquettes)
- **Statuts** : tag pills — ok (payé), warn (en attente), danger (en retard), neutral (brouillon), accent (envoyé) — cf. tableau section 10 du README design
- **Composants** : shell dans `components/layout/`, futurs composants shadcn dans `components/ui/`, métier dans `components/<domaine>/`
- **Sécurité (non négociable RNCP)** : RLS sur chaque table + `where: { userId: session.user.id }` sur chaque requête Prisma ; PDF généré côté serveur uniquement ; secrets dans `.env.local`
- **Éco-conception** : `select` Prisma explicites + pagination, composants lourds en `next/dynamic`, `next/image`, fonts `display: swap`

## Accessibilité (exigée)

Labels explicites liés (`for`/`id`), modales `role="dialog"` + `aria-modal` + focus trap + Échap, graphiques `role="img"` + `aria-label`, cartes radio `role="radiogroup"`, contrastes ≥ 4.5:1 dans les deux thèmes, navigation clavier complète, `prefers-reduced-motion` respecté.
